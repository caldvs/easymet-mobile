# EasyMet

A companion app for the Manchester Metrolink tram network — live departures, journey planning, service alerts, and a step-by-step on-trip view. Built in React Native + Expo.

```bash
npm install
npm run ios
```
