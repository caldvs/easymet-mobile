# EasyMet

A companion app for the Manchester Metrolink tram network — live departures, journey planning, service alerts, and a step-by-step on-trip view. Built in React Native + Expo, with an iOS 26 "Liquid Glass" floating tab bar and a hand-rolled soft-UI component kit.

```bash
npm install
npm run ios
```
